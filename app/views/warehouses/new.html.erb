<article>
  <header>
    <h2> <%= "#{ t 'new' } #{ t 'activerecord.models.warehouse' }" %> </h2>
  <section>
      <%= render partial: 'application/errors', locals: { model: @warehouse } %>
    </section>
  </header>

  <main>
      <%= render partial: 'form', locals: { warehouse: @warehouse } %>
  </main>
</article>